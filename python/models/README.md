# Models directory
# Trained model files (.pkl) are generated at runtime by train_model.py or main.py
# They are NOT committed to git (see .gitignore) to keep the repo lightweight.
# To pre-train before deploying:
#   python train_model.py --batches 200
